1. 입력받은 한글자를 대(소)문자로 바꿔서 저장
#include <ctype.h>
op = toupper(getchar());   // tolower()

goto 는 if로 제어가 불가능함

프로세스 과정

stack 세그먼트   지역변수  //함수가 부를때 마다 매번 초기화
  ex) num, i      (임시데이터, 재활용 공간)



heap 세그먼트   동적메모리 할당



data 세그먼트    전역변수, 정적변수  //한번만 초기화 //값 유지시킴
                     (재활용 공간 X)



code 세그먼트   함수의 실행코드
 ex) main()        (불변코드 영역)


* 첨자가 없는 배열변수는 그 배열이 할당된 메모리의 시작주소를 갖는 포인터 상수다. 중요!!!!!!!!!!!!!!!!!!!!!!!!!!! *

*과제*
525
546
210 ~ 213

github desktop
codeshare